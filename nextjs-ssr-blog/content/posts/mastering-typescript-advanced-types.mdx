---
title: 'Мастерство TypeScript: Продвинутые типы и паттерны'
description: 'Погрузитесь в продвинутые типы TypeScript, служебные типы и паттерны проектирования, которые повысят качество вашего кода.'
date: '2024-11-10'
coverImage: 'https://images.unsplash.com/photo-1516116216624-53e697fedbea?w=1200&h=630&fit=crop'
author: 'Анна Смирнова'
tags: ['TypeScript', 'JavaScript', 'Программирование', 'Типы']
categories: ['Обучение', 'TypeScript']
published: true
---

## Введение

Система типов TypeScript невероятно мощная и выходит далеко за пределы простых аннотаций типов. В этом руководстве мы изучим продвинутые типы и паттерны, которые сделают ваш код более надёжным и поддерживаемым.

## Служебные типы

TypeScript предоставляет несколько встроенных служебных типов, которые помогают преобразовывать типы.

### Partial и Required

```typescript
interface User {
  id: number
  name: string
  email: string
  age?: number
}

// Сделать все свойства необязательными
type PartialUser = Partial<User>

// Сделать все свойства обязательными
type RequiredUser = Required<User>
```

### Pick и Omit

```typescript
// Выбрать конкретные свойства
type UserPreview = Pick<User, 'id' | 'name'>

// Исключить конкретные свойства
type UserWithoutId = Omit<User, 'id'>
```

<Callout type="tip" title="Лучшая практика">
Используйте `Pick` и `Omit` вместо дублирования интерфейсов, когда вам нужны подмножества типов.
</Callout>

## Условные типы

Условные типы позволяют создавать типы на основе условий:

```typescript
type IsString<T> = T extends string ? true : false

type A = IsString<string>  // true
type B = IsString<number>  // false
```

### Практический пример

```typescript
type NonNullable<T> = T extends null | undefined ? never : T

type Result = NonNullable<string | null | undefined>  // string
```

## Типы шаблонных литералов

Создавайте типы на основе манипуляций со строками:

```typescript
type HTTPMethod = 'GET' | 'POST' | 'PUT' | 'DELETE'
type Endpoint = '/users' | '/posts' | '/comments'

type APIRoute = `${HTTPMethod} ${Endpoint}`
// Результат: 'GET /users' | 'GET /posts' | 'POST /users' | и т.д.
```

<Callout type="info">
Типы шаблонных литералов идеально подходят для создания типобезопасных маршрутов API и имён событий!
</Callout>

## Отображаемые типы

Преобразование существующих типов в новые:

```typescript
type Readonly<T> = {
  readonly [P in keyof T]: T[P]
}

type Nullable<T> = {
  [P in keyof T]: T[P] | null
}

interface Product {
  name: string
  price: number
}

type ReadonlyProduct = Readonly<Product>
type NullableProduct = Nullable<Product>
```

## Размеченные объединения

Создание типобезопасных конечных автоматов:

```typescript
type LoadingState = {
  status: 'loading'
}

type SuccessState<T> = {
  status: 'success'
  data: T
}

type ErrorState = {
  status: 'error'
  error: string
}

type AsyncState<T> = LoadingState | SuccessState<T> | ErrorState

function handleState(state: AsyncState<User>) {
  switch (state.status) {
    case 'loading':
      return 'Загрузка...'
    case 'success':
      return `Добро пожаловать, ${state.data.name}!`
    case 'error':
      return `Ошибка: ${state.error}`
  }
}
```

<Callout type="success">
Проверка исчерпываемости TypeScript гарантирует, что вы обработаете все случаи!
</Callout>

## Ограничения обобщений

Ограничивайте обобщённые типы для лучшей типобезопасности:

```typescript
interface HasId {
  id: number
}

function getById<T extends HasId>(items: T[], id: number): T | undefined {
  return items.find(item => item.id === id)
}

// ✅ Работает
getById([{ id: 1, name: 'Алиса' }], 1)

// ❌ Ошибка: объект не имеет 'id'
// getById([{ name: 'Боб' }], 1)
```

## Ключевое слово infer

Извлечение типов из других типов:

```typescript
type ReturnType<T> = T extends (...args: any[]) => infer R ? R : never

function getUser() {
  return { id: 1, name: 'Алиса' }
}

type User = ReturnType<typeof getUser>  // { id: number; name: string }
```

<Callout type="warning">
Ключевое слово `infer` мощное, но может сделать типы сложнее для чтения. Используйте его разумно!
</Callout>

## Заключение

Продвинутая система типов TypeScript позволяет вам писать более безопасный и выразительный код. Освоив эти паттерны, вы будете отлавливать больше ошибок на этапе компиляции и улучшите свой опыт разработки.

Продолжайте исследовать и удачной типизации! 💙
