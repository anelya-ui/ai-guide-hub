---
title: 'CSS Grid: Полное руководство по современным макетам'
description: 'Освойте CSS Grid и создавайте сложные адаптивные макеты с лёгкостью. Изучите основы и продвинутые техники 2024 года.'
date: '2024-11-01'
coverImage: 'https://images.unsplash.com/photo-1507721999472-8ed4421c4af2?w=1200&h=630&fit=crop'
author: 'Елена Васильева'
tags: ['CSS', 'Веб-дизайн', 'Frontend', 'Вёрстка']
categories: ['Обучение', 'CSS']
published: true
---

## Что такое CSS Grid?

CSS Grid — это двумерная система компоновки, которая позволяет создавать сложные макеты с рядами и столбцами. В отличие от Flexbox (который одномерный), Grid отлично справляется с созданием как рядов, ТАК И столбцов одновременно.

<Callout type="info">
CSS Grid поддерживается во всех современных браузерах с 2017 года!
</Callout>

## Базовый Grid контейнер

```css
.container {
  display: grid;
  grid-template-columns: 200px 200px 200px;
  grid-template-rows: 100px 100px;
  gap: 20px;
}
```

Это создаёт сетку 3x2 с:
- 3 столбца по 200px каждый
- 2 ряда по 100px каждый
- 20px промежуток между элементами

## Дробные единицы (fr)

Единица `fr` представляет собой долю доступного пространства:

```css
.container {
  display: grid;
  grid-template-columns: 1fr 2fr 1fr;
}
```

<Callout type="tip">
Единицы `fr` по своей природе адаптивны - они автоматически подстраиваются под размер контейнера!
</Callout>

## Функция `repeat()`

Легко создавайте множественные дорожки:

```css
/* Вместо: 1fr 1fr 1fr 1fr */
grid-template-columns: repeat(4, 1fr);

/* Смешивание значений */
grid-template-columns: 200px repeat(3, 1fr) 200px;
```

## Области Grid

Именуйте области grid для семантичных макетов:

```css
.container {
  display: grid;
  grid-template-areas:
    "header header header"
    "sidebar main main"
    "footer footer footer";
  grid-template-columns: 200px 1fr 1fr;
  grid-template-rows: auto 1fr auto;
}

.header { grid-area: header; }
.sidebar { grid-area: sidebar; }
.main { grid-area: main; }
.footer { grid-area: footer; }
```

<Callout type="success">
Области Grid делают ваш CSS более читаемым и лёгким в поддержке!
</Callout>

## Адаптивные сетки с `auto-fit`

```css
.gallery {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 1rem;
}
```

Это создаёт адаптивную сетку, которая:
- Помещает столько столбцов по 250px, сколько возможно
- Расширяет столбцы для заполнения оставшегося пространства
- Автоматически переносит на новые ряды

<Callout type="warning">
Используйте `auto-fit` (а не `auto-fill`), если хотите, чтобы столбцы расширялись при наличии дополнительного пространства.
</Callout>

## Свойства выравнивания

### Align Items (вертикальное)

```css
.container {
  align-items: start;    /* сверху */
  align-items: center;   /* по центру */
  align-items: end;      /* снизу */
  align-items: stretch;  /* по умолчанию */
}
```

### Justify Items (горизонтальное)

```css
.container {
  justify-items: start;   /* слева */
  justify-items: center;  /* по центру */
  justify-items: end;     /* справа */
  justify-items: stretch; /* по умолчанию */
}
```

## Размещение элементов

Вручную размещайте элементы на сетке:

```css
.item {
  grid-column: 1 / 3;  /* Занимает от столбца 1 до 3 */
  grid-row: 1 / 2;     /* Занимает от ряда 1 до 2 */
}

/* Сокращённая запись */
.item {
  grid-area: 1 / 1 / 2 / 3;
  /* начало-ряда / начало-столбца / конец-ряда / конец-столбца */
}
```

## Продвинутый пример: Макет карточек

```css
.card-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 2rem;
  padding: 2rem;
}

.card {
  display: grid;
  grid-template-rows: 200px 1fr auto;
  border-radius: 8px;
  overflow: hidden;
  box-shadow: 0 2px 8px rgba(0,0,0,0.1);
}

.card-image {
  object-fit: cover;
  width: 100%;
  height: 100%;
}

.card-content {
  padding: 1.5rem;
}

.card-footer {
  padding: 1rem 1.5rem;
  border-top: 1px solid #eee;
}
```

## Grid vs Flexbox

Когда что использовать?

| Используйте Grid когда... | Используйте Flexbox когда... |
|----------------------------|------------------------------|
| Вам нужны 2D макеты (ряды И столбцы) | Вам нужны 1D макеты (ряд ИЛИ столбец) |
| Вы знаете структуру заранее | Контент должен определять макет |
| Создание макетов страниц | Выравнивание элементов в компоненте |

<Callout type="tip" title="Лучшая практика">
Используйте Grid для макетов страниц, Flexbox для макетов компонентов. Они прекрасно дополняют друг друга!
</Callout>

## Инструменты разработчика в браузере

Современные браузеры имеют отличные инструменты отладки Grid:

1. Откройте DevTools (F12)
2. Нажмите на значок grid рядом с `display: grid`
3. Увидите линии сетки, промежутки и области, наложенные на вашу страницу

## Заключение

CSS Grid — это мощный инструмент для создания современных веб-макетов. С такими возможностями, как дробные единицы, области grid и автоматическое размещение, вы можете создавать адаптивные дизайны, которые ранее были невозможны или требовали хаков.

Начните использовать Grid сегодня и повысьте свои навыки CSS! 🎨
